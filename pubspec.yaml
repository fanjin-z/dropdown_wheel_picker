name: dropdown_wheel_picker
description: Flutter package for dropdown scrollable wheel picker. Support many use cases incl. multi-cols items, date, time, distance.
version: 0.2.0
repository: https://github.com/fanjin-z/dropdown_wheel_picker
homepage: https://pub.dev/packages/dropdown_wheel_picker
issue_tracker: https://github.com/fanjin-z/dropdown_wheel_picker/issues

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
      sdk: flutter

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0