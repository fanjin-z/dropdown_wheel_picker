name: dropdown_wheel_picker
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
      sdk: flutter

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
